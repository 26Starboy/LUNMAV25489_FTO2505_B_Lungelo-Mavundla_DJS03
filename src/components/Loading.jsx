import React from "react";

/**
 * Loading component displays a spinner or loading message.
 */
const Loading = () => {
  return <div className="loading">Loading podcasts...</div>;
};

export default Loading;
